apiVersion: v1
kind: Service
metadata:
  name: {{ include "devpanel-static.fullname" . }}
  labels:
    {{- include "devpanel-static.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devpanel-static.selectorLabels" . | nindent 4 }}
